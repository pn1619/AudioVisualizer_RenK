# macOS development dependencies. Installs macOS runtime deps too.
-r requirements-mac.txt

ruff>=0.6          # lint + import sort
black>=24.0        # formatter
mypy>=1.11         # static type checking (optional/non-blocking at first)
pytest>=8.0        # tests (run headless with SDL_VIDEODRIVER=dummy)
pre-commit>=3.8    # git hook running ruff + black on commit
pillow>=10.0       # tools/prep_icon.py only (bakes the app icon); not a runtime dep
pyinstaller>=6.0   # builds the macOS .app bundle (tools/mac/build-app.sh)
